{
  "name": "x-chat",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run --watch index.ts",
    "build": "bun build index.ts --outdir ./out"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "htmx": "^0.0.2",
    "sqlite": "^5.1.1"
  }
}